/**
 * Created by james on 09/06/2017.
 */
var jsgui = require('../lang/lang');
var stringify = jsgui.stringify;

var fs2 = require('../server/fs2');

// delete git directories as well?
//  This should be for copying the files from machine to machine, and getting the most concise ZIPs of files






// recursive dir delete.
//  walks through, has a callback for each directory, true or false function to see if it should be deleted.







fs2.recursive_dir_delete('../', function(path) {
    console.log('path', path);

    if (path.indexOf('/node_modules/') > -1) {
        return true;
    }
    return false;
},
    function(err, res) {
    if (err) {
        throw err;
    } else {
        console.log('res', stringify(res));
    }
})
